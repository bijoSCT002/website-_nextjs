import React from "react";
import { ProjectDetail } from "../../../ProjectDetail";

/** Project detail page – uses existing ProjectDetail (no change to existing component) */
export function ProjectDetailPage() {
  return <ProjectDetail />;
}
