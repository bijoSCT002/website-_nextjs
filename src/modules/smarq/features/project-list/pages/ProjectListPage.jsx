import React from "react";
import { SmarqMain } from "../../../SmarqMain";

/** Project list page – uses existing SmarqMain (no change to existing component) */
export function ProjectListPage() {
  return <SmarqMain />;
}
